{
    "name": "hiking-trail",
    "version": "1.0.0",
    "description": "徒步旅行路線記錄應用",
    "scripts": {
      "start": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
      "install-all": "npm install && cd client && npm install && cd ../server && npm install"
    },
    "devDependencies": {
      "concurrently": "^8.2.2"
    }
  }